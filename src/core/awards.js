// Награды: за что игра признаёт заслуги.
//
// Три источника, намеренно разного веса. Игра — пол: заработать может каждый,
// суммы небольшие, и нужны они затем, чтобы новичок вообще увидел отклик.
// Спидран платит больше за штуку и куда труднее повторяется — первое место
// на доске одно. Авторство платит больше всех, потому что это единственное,
// что делает что-то для других, и потому что альтернатива — игра, где все
// играют и никто не строит.

import { api } from './api.js'

/** Что заработано и что осталось заработать. */
export const myAwards = () => api.get('/api/me/awards')

/** Общая таблица — единственная, что охватывает всё, а не одну историю. */
export const ranking = () => api.get('/api/ranking')
